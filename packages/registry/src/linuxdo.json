{
  "title": "Linux.do",
  "category": "forum",
  "defaults": {
    "cache": "5m",
    "vars": {
      "origin": "https://linux.do"
    },
    "loader": {
      "type": "json",
      "items": "topic_list.topics[?visible && !archived && !pinned]",
      "fields": {
        "title": "title",
        "url": {
          "select": "id",
          "template": "{{ source.vars.origin }}/t/topic/{{ scope.value | url_path }}"
        },
        "inline": {
          "icon": {
            "template": "{% assign user = scope.response.json.users | where: 'username', scope.item.last_poster_username | first %}{% if user and user.avatar_template %}{{ user.avatar_template | replace: '{size}', '48' | absolute_url: source.vars.origin }}{% endif %}"
          }
        }
      }
    },
    "metadata": {
      "color": "slate",
      "home": "https://linux.do"
    }
  },
  "sources": {
    "latest": {
      "metadata": {
        "title": "最新",
        "type": "timeline"
      },
      "loader": {
        "url": "{{ source.vars.origin }}/latest.json?order=created",
        "fields": {
          "timestamp": {
            "select": "created_at",
            "template": "{{ scope.value | date_to_ms }}"
          }
        }
      }
    },
    "top-daily": {
      "metadata": {
        "title": "热门",
        "type": "hottest"
      },
      "loader": {
        "url": "{{ source.vars.origin }}/top/daily.json"
      }
    }
  }
}
