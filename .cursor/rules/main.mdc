---
description:
globs:
alwaysApply: true
---

ALWAYS use English for all text, including comments.
DONT create markdown documents, not needed. Tell me directly, PLEASE.

ALWAYS use TypeScript, React v19, Tailwind CSS v4, Bun.

ALWAYS use `bun run test` to run tests if needed.

1. 原来使用了 asChild prop, 这是 Radix UI 的模式
2. 这个项目使用的 ui/* 组件来自 @base-ui/react,
3. render prop 接受一个元素（如 Link） 而 Button 组件会注入到该元素中